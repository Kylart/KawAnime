language: node_js

node_js:
  - "10"
  - "9"

install:
  - npm ci

script:
  - npm run build
  - npm run test
  - npm run cloc
after_success: npm run coverage

cache:
  directories:
    - "node_modules"
    - "~/.npm"
    - "~/.cache"

notifications:
  email: false
